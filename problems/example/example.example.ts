import { inputs, outputs } from "./data";

// Remove comments to see the type errors
function findLongestArray(arrays: any[][])/*: any[]*/ {
  // Your TypeScript code here
}

export default {
  inputs,
  outputs,
  function: findLongestArray,
};
